# Wallet seed — BIP39 mnemonic phrase OR 64+ char hex seed
SEED=

# Network: "preview" | "preprod" | "standalone"
NETWORK=preview

# Proof server URL (optional, defaults per network)
PROOF_SERVER=

# Debug log level (optional)
DEBUG_LEVEL=
